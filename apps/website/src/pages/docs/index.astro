---
import SiteLayout from "../../layouts/SiteLayout.astro";
import { DocsPage } from "../../routes/docs/DocsPage";
import { findSeoRoute } from "../../seo/routeCatalog";

const DEFAULT_DOC_SLUG = "getting-started";
const route = findSeoRoute("/docs");
---

<SiteLayout
  title={route.title}
  description={route.description}
  canonicalPath={route.canonicalPath}
  indexable={route.indexable}
>
  <main id="top" class="route-main docs-route">
    <DocsPage slug={DEFAULT_DOC_SLUG} client:load />
  </main>
</SiteLayout>

