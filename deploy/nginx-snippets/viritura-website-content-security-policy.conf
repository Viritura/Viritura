# Astro and the public Storybook emit build-derived script/style hashes in CSP
# meta elements. Keep frame ancestry in an HTTP header because CSP meta cannot
# enforce frame-ancestors, and do not intersect their generated script policies.
add_header Content-Security-Policy "frame-ancestors 'self'" always;